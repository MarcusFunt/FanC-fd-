# Core dependencies
numpy>=1.24
scipy>=1.10
trimesh>=4.0
pydantic>=2.0
pyyaml>=6.0
pandas>=2.0
matplotlib>=3.7
fastapi>=0.111
uvicorn[standard]>=0.30

# Optional: faster mesh operations
# rtree>=1.0          # spatial indexing for trimesh
# shapely>=2.0        # 2D geometry ops

# Development / testing
pytest>=7.0
pytest-cov>=4.0
ruff>=0.1
mypy>=1.0
